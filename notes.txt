the null object pattern 

use the null object pattern for the data that comes from directus to prevent checking for things like translations and unknown content types

use the null object pattern for the fares data wich allows to do Marco requests

builder object

helps to dynamically build object in no specific order.

